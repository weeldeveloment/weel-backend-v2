# Python
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
__pycache__/
.claude/
.codegraph/
.gemini/
.kiro/
opencode.jsonc


# Virtual Environment
.venv/
venv/
ENV/
env/
.env.local
.env.*.local
.env.*

# Environment variables (sensitive)
.env
.env.staging
.env.server
.env.production

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Django migrations
# Track migration files in git (required for deploy DB schema).
# Only ignore migration caches.
**/migrations/__pycache__/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-project
*.sublime-workspace

# Testing
.coverage
.coverage.*
htmlcov/
.tox/
.pytest_cache/
.hypothesis/

# Poetry
poetry.lock

# Celery
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# Misc
*.pyo
.mypy_cache/
.dmypy.json
dmypy.json

# OS
Thumbs.db
.DS_Store

# Project specific
logs/
media_backup/
*.pid
media/*

# Postman
postman_tests/
postman/
# (do not ignore migrations folders)

scripts/

# monitoring/ is committed: the cloud monitoring routine reads RUNBOOK.md from git.
# Its own .gitignore keeps .env / tokens out.

.cursor/

design

docs

# AI tool configs (local)
.claude/
.gemini/
.kiro/
.codegraph/

GEMINI.md
AGENTS.md
.mcp.json
graphify-out/

# Credentials — Firebase service account, Apple APNs key.
# These were committed once; the keys they contained must be treated as
# compromised and rotated. Never add them back.
certificates/
*.p8
dump.rdb

# Frontend tip generatsiyasi uchun eksport qilinadigan sxemalar — artefakt,
# `make export-schema` yoki frontendlarning gen skriptlari qayta yaratadi.
# Deploy kontraktining "yodda saqlangan" nusxasi esa tools/api-baseline/ da
# turadi va u ataylab commit qilinadi.
/openapi.json
/b2b-openapi.json
tools/api-baseline/.fresh.json
