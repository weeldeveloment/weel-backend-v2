apiVersion: 1

providers:
  - name: weel
    orgId: 1
    folder: Weel
    folderUid: weel
    type: file
    disableDeletion: false
    allowUiUpdates: false     # manba — git; UI'da o'zgartirsangiz JSON'ni export qilib commit qiling
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
